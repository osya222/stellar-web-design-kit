
export interface Product {
  id: number;
  name: string;
  price: number;
  category: string;
  manufacturer: string;
  description?: string;
  image?: string;
  size?: string;
  weight?: string;
  packaging?: string;
  catchDate?: string;
}
