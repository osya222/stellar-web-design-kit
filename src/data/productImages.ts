
import { Product } from "@/types/product";

// Simple placeholder function that returns undefined since we've removed image functionality
export const getProductImage = (product: Product): string | undefined => {
  return undefined;
};
